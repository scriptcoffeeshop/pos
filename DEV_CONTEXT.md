# 開發交接紀錄

更新日期：2026-05-12

## 目前狀態

- 專案位置：`/Users/kimi/Library/Mobile Documents/com~apple~CloudDocs/POS`
- 技術基底：Vue 3 + Vite + TypeScript。
- iCHEF 請款明細：2026-05-12 以 Safari / Computer Use 讀取官方「提供請款明細供消費者確認結帳內容」，確認出單後顧客聯不包含折扣，結帳前給客人核對折扣後金額時應列印請款明細；請款明細包含商品、註記、服務費/清潔費/場地費、折扣總計與折扣後總計。`src/lib/printing.ts` 新增 `buildBillingStatementPayload()`，交易明細改共用折扣後 footer，顧客聯改保留折扣前 footer；付款頁可直接列印「請款明細」，訂單中心會依付款狀態把同一按鈕標示為未收款「請款明細」或已收款「交易明細」。此功能不新增資料表，直接讀既有 `orders` 金額欄位。
- iCHEF 重印顧客聯：2026-05-12 以 Safari / Computer Use 讀取官方「同單多張顧客聯合併列印」，確認每次加點出單的顧客聯只包含當次出單商品與小計，多次加點後應在點餐頁用「重印顧客聯」整理同單完整顧客聯。Script Coffee POS 在已有 `counterDraftOrderId` 的點餐票券底部顯示顧客聯圖示；點擊後先 `saveCounterOrder(false)` 保存目前票券，再呼叫既有 `printCustomerReceipt(order.id)` 建立同一張訂單的顧客聯 print job。此功能不新增資料表，沿用 `orders.print_jobs`、`buildCustomerReceiptPayload()` 與 Android LAN printer 流程。
- iCHEF 出單排序：2026-05-12 以 Safari / Computer Use 讀取官方「調整排序讓出單順序符合店內營運流程」，確認 iCHEF 印單順序為出單機排序優先，其次是同一出單機內印單規則排序，且規則只能在同一出單機內調整位置。本專案不新增資料表，直接保存 `printer_settings.stations` 與 `printer_settings.rules` 的陣列順序；前台列印工作區與後台出單規則都提供上下箭頭排序。`src/lib/printing.ts` 的 `buildOrderPrintPlan()` 改為 station-first 建立 print jobs，未啟用或未開自動列印的出單機會整站略過。
- iCHEF App 主機 / 子機模式：2026-05-12 以 Safari / Computer Use 讀取官方「iCHEF POS App 運作模式」，確認同一 Store ID 僅一台 iPad 可作主機，子機由主機提供資料、不可套用新設定檔，且工具箱只開放錢櫃管理、交易查詢與作廢、裝置管理、小結、線上點餐功能設定與線上訂位黑名單。本專案新增 `engagement_settings.appOperation`，包含 `hostStationId`、`childStationIds` 與 `maxChildStations`；後台「iCHEF 補齊」可維護主機與子機 station id。`usePosSession()` 會依目前 station 判斷主機/子機，子機停用 topbar、工具箱與系統資訊的「套用新設定檔」，工具箱首頁也會停用主機限定卡片，只保留交易查詢與作廢、錢櫃管理、裝置管理、小結與系統資訊。未指定主機時視為主機相容模式，因此舊平板或 fresh reinstall 後不會被突然鎖住；這個設定只放在既有 `engagement_settings` JSON，不新增資料表或 localStorage 共享狀態。
- iCHEF App 套用新設定檔：2026-05-12 以 Safari / Computer Use 讀取官方「App 套用新設定檔｜iCHEF POS」，確認後台設定可先調整，POS App 只有選擇套用新設定檔時才下載最新後台設定。本專案在 `usePosSession()` 以 station-local `script-coffee-pos-applied-settings-profile:*` 保存每台平板最後已套用的 runtime 快照與 fingerprint；首次載入若本機已套用版本和伺服器 runtime 不同，先套用本機快照並把伺服器版本標為待套用。Realtime/polling 收到新的 runtime 後不會立即覆蓋商品、線上、列印、桌況、外觀、折扣、權限與互動設定，topbar、工具箱與系統資訊會顯示「新設定檔」並由操作員按「套用新設定檔」後才保存新快照。訂單、班別、心跳、提醒狀態與商品清單仍照常從 `pos-api` 同步，localStorage 只保存本平板已套用 runtime 版本，不作為共享資料來源。
- iCHEF 商品條碼點餐：2026-05-12 以 Safari / Computer Use 讀取官方「商品條碼點餐｜iCHEF POS」，確認條碼在後台商品管理手動設定、不可重複、上限 48 字元，支援半形英數字與 `-.$/+%:`，且只用於點餐頁商品掃描。`20260512250000_add_product_barcode_ordering.sql` 新增 `products.barcode`、格式 check 與非空唯一索引；`pos-api` 的商品查詢、建立與更新 payload 會正規化條碼為大寫並保存。後台「商品菜單」可編輯與搜尋條碼；POS 點餐頁右上角新增條碼掃描入口，相機支援時用 `BarcodeDetector` + `getUserMedia` 掃描，不支援時保留掃碼器/手動輸入。掃到可售商品會用既有 `selectMenuItem()` 流程加入票券；有註記或套餐時會開啟選項面板，停售、售完或暫停供應會阻擋加入。
- iCHEF 移桌與併單：2026-05-12 以 Safari / Computer Use 讀取官方「將訂單移桌或併單」，確認內用訂單可拖到空桌移桌，也可拖到另一張訂單併單；併單後來客數加總，且出單規則可針對移桌/併單時機列印。Script Coffee POS 保留既有 `/orders/:id/floor` 移桌流程，並新增 `POST /orders/:id/merge`：API 會檢查來源與目標都是未完成內用單、沒有已記錄拆單/混合支付與發票活動，合併 `draft_lines` 或 `order_items`、加總金額/費用/折抵/點數/標籤與備註來客數，來源單標成 `voided/failed` 並寫 `order.merge` 稽核。桌位地圖右側現在會列出可併入的其他桌位；操作後來源桌清空、目標桌人數加總。`PrintRuleTiming` 新增 `move`、`merge`，前後台印單規則可勾選「移桌」「併單」，預設規則仍只包含 `order/reprint`。
- iCHEF 出餐管理：2026-05-12 以 Safari / Computer Use 讀取官方「出餐管理」，確認出餐可用顧客聯手寫註記，或在 POS 開啟「出餐確認」後逐品項標記出餐完成；未全數完成時訂單維持待處理色，全部完成後轉完成色。`20260512252000_add_order_item_fulfillment.sql` 在 `order_items` 新增 `fulfilled_at` 與 `fulfilled_by_station_id`，`pos-api` 新增 `PATCH /orders/:id/items/:itemId/fulfillment`，會走現有 station claim/lease、寫回明細、回傳完整訂單並記錄 `order.item.fulfillment` 稽核。前台訂單中心展開「明細」時，若 `engagement_settings.workflowAlerts.fulfillmentConfirmationEnabled=true`，每個正式品項會顯示「待出餐 / 已出餐」；所有明細完成後前端呼叫既有狀態流程把訂單推到 `ready`，取消任一完成品項則從 `ready` 回到 `preparing`。出餐確認關閉時仍維持首次出單後自動可交付。
- iCHEF 電子發票帳本：2026-05-12 以 Safari / Computer Use 讀取官方「iCHEF 電子發票與交易明細」「由財政部認證的 iCHEF 電子發票服務」「使用 iCHEF 開立電子發票」，確認 iCHEF 支援紙本發票、統編、載具、捐贈碼、多元支付發票開關、開立/作廢/申報與 48 小時上傳時限，且全零稅商品開立發票須選擇銷售原因。本專案新增 `20260512223000_add_order_electronic_invoice_ledger.sql`，在 `orders` 保存 `invoice_donation_code`、`electronic_invoice_requested/status/print_mode/number/random_code/issued_at/voided_at/upload_due_at`，並在 `engagement_settings.electronicInvoice` 保存啟用、結帳預設開立、手動調整、紙本列印與上傳期限；`20260512255000_add_zero_tax_sales_reason.sql` 追加 `orders.zero_tax_sales_reason`。POS 付款頁新增電子發票區塊、捐贈碼欄位與零稅銷售原因選單；線上點餐設定新增 `showDonationCodeField`；交易查詢、訂單明細、線上新單提醒與列印 payload 會顯示捐贈碼、零稅原因與發票狀態。`pos-api` 會在建單/finalize 保存發票需求，並在整筆訂單商品皆為零稅且要開電子發票時拒絕缺少銷售原因的送單；收款後把發票排入 `queued` 並計算上傳期限，未收款作廢標成 `voided`，已收款退款標成 `refunded`。目前只做可追蹤帳本與現場列印資訊，尚未串接財政部加值中心憑證或正式字軌申報。
- iCHEF 電子發票開立紀錄與商品稅別：2026-05-12 以 Safari / Computer Use 讀取官方「[報表分析] 電子發票開立紀錄」與「啟用電子發票功能」，確認後台報表可查近 2 年、建議單次不超過 93 天，並用結帳時間、載具/捐贈碼、銷售額、營業稅、零稅/免稅額、買方統編與目前概況檢視發票紀錄；商品預設稅別為應稅 TX 5%，單品可改不同稅率。`20260512254000_add_product_tax_category.sql` 新增 `products.tax_category` 與 `order_items.tax_category`，後台商品菜單可設定應稅 TX 5%、零稅或免稅，POS/線上/草稿送單會保留品項稅別快照。`pos-api` 的 `GET /admin/reports/electronic-invoices` 用 `orders.electronic_invoice_*` 帳本、`orders.zero_tax_sales_reason` 與 `order_items.tax_category/line_total` 產生報表，支援 `startDate/endDate/status/serviceMode/source/minPartySize/maxPartySize`，查詢時間以 `electronic_invoice_issued_at` 優先、未開立時回退 `updated_at`。後台「營運報表」新增電子發票開立紀錄區塊與 CSV 匯出，列出結帳時間、來源、服務方式、人數、載具/捐贈碼或統編、銷售額、營業稅、零稅額、零稅原因、免稅額、發票號碼、隨機碼、上傳期限與開立/作廢/退款/失敗狀態。
- iCHEF 僅菜單瀏覽：2026-05-12 以 Safari / Computer Use 觀察 iCHEF 後台「內用掃碼點餐」功能狀態，確認狀態可設為「無點餐功能，僅菜單瀏覽」。本專案沿用 `online_ordering.enabled=false` 對應此模式，不新增 migration；後台「線上點餐」會以「功能狀態 / 僅菜單瀏覽」呈現，開關文字改為「消費者可送出訂單」，消費者線上/QR 頁仍載入商品、店家資訊與提醒事項，但送出按鈕改為「僅菜單瀏覽」且不可送單。`pos-api` 保留同一層 `onlineOrdering.enabled` 檢查，繞過前端的 online/qr 建單仍回 409。
- iCHEF 線上訂單通知：2026-05-11 以 Safari 讀取官方知識庫「線上點餐功能設定｜iCHEF POS」，確認訂單通知是每台 iPad 分開設定，且內用掃碼可指定桌位通知。`online_ordering.notificationRouting` 新增 `stations[]`，保存每台平板的通知開關、服務方式、內用桌位清單、提示聲模式與音量；POS 桌位地圖右側「線上通知設定」可直接修改目前平板設定。`usePosSession()` 前景提醒、Web 背景通知與 Android `OnlineOrderNotifier` / `OnlineOrderPollingService` 背景輪詢都會依目前 station 套用路由；舊 runtime 由 `20260512163000_add_online_notification_routing_settings.sql` 補成空 stations，未設定的平板維持接收全部新單。
- iCHEF 接單選項：2026-05-12 以 Safari / Computer Use 進入 iCHEF 網頁版後台「雲端餐廳 > 內用掃碼點餐」，確認接單選項支援接單畫面選擇「接受但不出單」。Script Coffee POS 沿用既有 `online_ordering.acceptWithoutPrinting`，後台線上點餐設定改為「接單時可接受但不出單」；待接單清單、訂單內容彈窗與浮動提醒在啟用後會同時提供「接單並出單」與「接受不出單」。`acceptOnlineOrderForStation(orderId, { printAfterAccept })` 會共用 claim 與 shared reminder state，只有 `printAfterAccept=true` 才執行 `printOrder(order.id, 'order')`，因此 fresh reinstall 後仍以 Supabase 訂單狀態與 shared reminder state 還原，不新增 migration。
- iCHEF 線上付款拒絕退款：2026-05-12 以 Safari / Computer Use 檢查 iCHEF 後台「內用掃碼點餐 > 支付模組設定」，確認線上付款被拒絕或取消時會自動退款。`rejectOnlineOrderForStation()` 現在會依付款狀態分流：待收款訂單維持 `voidOrder(..., '拒絕接單')`；已授權或已付款訂單改呼叫 `refundOrder(..., '拒絕接單')`，由 `/orders/:id/refund` 與 `refund_pos_order()` 把狀態改為 `voided/refunded`、建立退款流水，並觸發既有會員券、點數與電子發票終態同步。拒絕後仍會寫入 `online_order_reminder_states`，避免其他平板或 fresh reinstall 後重複提醒。
- iCHEF 優惠/點數現場付款：同一個 iCHEF 後台「支付模組設定」也提示優惠活動與點數活動須選擇現場付款，於 iCHEF POS 結帳時操作。`ConsumerOrderPage` 現在會在 `onlineDiscountAmount > 0` 時把可選付款方式限制為 `cash`；若目前是外送或 QR 先結這類必須線上付款的服務方式，會顯示阻擋訊息而不送單。`pos-api` 新增 `onlineBenefitsRequireOnsitePayment()`，會用重新計算後的自動優惠、`pointsRedeemed` 與 `couponCode` 再驗證一次，非 `cash` 送單回 409，因此舊頁或 API 不能用線上付款繞過優惠/點數現場結帳規則。
- iCHEF 多結帳口：2026-05-12 以 Safari 讀取官方知識庫「iPad 子機升級為獨立結帳櫃檯｜多結帳口」，確認子機可升級為獨立結帳口，且訂單/付款資訊會依帳本分離。`engagement_settings.checkoutCounters` 新增啟用開關、預設帳本與多個帳本設定；後台「iCHEF 補齊」可指定帳本名稱、iPad station id、出單機、錢櫃與刷卡/掃碼 device id。`20260512193000_add_multi_checkout_register_books.sql` 在 `register_sessions` 新增 `book_id/book_name/station_id`，在 `orders` 新增 `register_session_id/checkout_station_id/checkout_book_id`；`pos-api` 的 `/register/current/open/close`、現金臨時收支、錢櫃事件、建單、草稿 finalize 與收款都會依目前 station 分配帳本，關帳摘要改以 `register_session_id` 彙總，未指定的平板使用預設帳本，不用 localStorage 保存帳本狀態。
- iCHEF 店面流程與外帶設定：2026-05-12 以 Safari / Computer Use 觀察 iCHEF 後台「店面管理 > 店面流程與警示」，確認有出餐確認、今日訂單開始/結束時間、外帶預設取餐時間、外帶循環模式與候位/未出單/出餐等待/店內滯留/外帶等待/預約取餐提前提醒等警示。本專案先補 `engagement_settings.workflowAlerts`：後台「iCHEF 補齊」可設定今日訂單查詢區間、出餐確認、預約取餐提前提醒、到點提醒提前分鐘、預設外帶取餐分鐘、各區警示開關與分鐘數、外帶循環與自動出店；POS 外帶草稿會預帶 `requestedFulfillmentAt`，訂單工作區今日/未來/過去依查詢區間切分，出餐確認關閉時首次出單後自動標記可交付，警示開啟後會在候位列、桌位卡、訂單列與「流程警示」任務卡標示超時，啟用外帶循環後送單或結帳會直接開下一張外帶單。舊 runtime 由 `20260512233000_add_workflow_alert_runtime_settings.sql`、`20260512241000_add_fulfillment_confirmation_runtime_setting.sql` 與 `20260512242000_add_workflow_warning_threshold_settings.sql` 補上預設 `00:00`、`23:59`、15 分鐘提醒、5 分鐘外帶取餐、出餐確認關閉與各警示關閉。
- iCHEF 點餐頁顯示設定：同一個 iCHEF 後台頁面底部「點餐頁顯示設定」顯示 `註記欄位數量`，可選每列 1、2、3 欄，Script Coffee 目前為每列 3 欄。本專案新增 `engagement_settings.orderPageDisplay.noteColumns` 與 `20260512234500_add_order_page_display_runtime_settings.sql`，後台「iCHEF 補齊」提供相同 1/2/3 欄選項，POS 商品註記與套餐子商品註記格線用同一份 runtime 控制；消費者線上點餐維持原本自適應格線，避免把門市 iPad 顯示設定套到公開頁。
- 消費者線上訂位：`?view=reservation` 會載入 `ConsumerReservationPage`，使用 `/settings/runtime` 的 `engagementSettings.reservationWebsite` 作為唯一設定來源。後台「iCHEF 補齊」新增「專屬訂位網站 / 規則」，可儲存開關、餐廳資訊、訂位公告、人數上下限、訂位間隔、用餐時間、提前時間、開放天數、每週開放時段與 iCHEF「特殊時間設定」對應的 `specialDates`；特殊訂位日可設定日期/日期區間整日不開放，或用自訂開放時段取代固定時段，跨夜時段也由公開 API 和消費者頁同時計算。公開 `POST /reservations` 會用同一份 runtime 規則驗證後寫入 `reservations`，並用 `reservation_blacklist_entries.normalized_phone` 阻擋黑名單手機，localStorage 不承擔訂位規則或黑名單狀態。
- 訂位容量控管：`reservationWebsite` 現在也保存 `seatHoldMinutes`、`allowTableCombinations` 與 `onlineTableIds`。公開 `/reservations` 會同時載入 `floor_plan`，依可線上訂位桌位、桌位容量、既有 booked/reminded/confirmed/seated 訂位、用餐時間與座位保留時間分配 `reservations.assigned_table_ids`；如果既有訂位沒有 assigned table，API 會用 party size 保守占用容量。後台訂位列表與消費者確認畫面都會顯示已保留桌號。
- POS 訂位管理：工具箱新增「訂位管理」工作區，對照 iCHEF POS 的日/週/月訂位列表、提醒標示、特殊訂位日標籤、消費者通知狀態、修改訂位與帶位開單。POS 會用 `/admin/reservations` 依目前檢視區間同步 Supabase 訂位，支援狀態篩選、新增訂位、排多桌、修改日期時間/人數/桌位/狀態/訂位人資訊/標籤/店內備註、已發送提醒、已保留訂位、取消、未出席與帶位開單；畫面會標示遲到、未排桌、座位數不足、桌位重疊、特殊訂位日與不開放訂位。修改訂位提供前/後一個訂位時段快捷鍵，和桌位選擇共用 `floor_plan`。帶位開單會在訂位時間前 2 小時起可按，建立內用草稿單、帶入顧客電話/備註/人數/標籤/保留桌位，並把訂位狀態更新為 `seated`。`pos-api` 會在 POS/後台同步訂位與公開訂位分配桌位前，依台北時間把隔兩天凌晨 1 點已過且仍為 booked/reminded/confirmed 的訂位自動更新為 `no_show`，並寫入 `reservation.auto_no_show` 稽核事件。桌位地圖會從已載入的 booked/reminded/confirmed 訂位顯示下一組訂位時間。
- 初始畫面：門市 POS 工作台預設進入接近 iCHEF Server 的「桌位地圖」，固定右側導航列已移除，工具箱改為可自由拖曳的浮動入口，並保留上一版綠色主色系。桌位地圖提供內用與外帶/外送第一層切換，支援多樓層切換且每個樓層可有不同桌數、桌號、大小與位置，內建 A1/A2/A3 桌卡、空桌/進行中/可交付/鎖定狀態、人數、金額、未出單等待、桌內滯留、候位清單、桌位顯示設定、移桌與通知中心；選桌只會選取桌位，右側控制區才會開桌點餐、開啟訂單、移桌或清桌，避免誤觸建立訂單。連點工具箱 6 下進入後台編輯模式後，可新增/刪除樓層、新增/刪除桌位、修改桌號/容量、拖曳桌位位置並調整桌子大小。外帶/外送頁保留搜尋/篩選訂單，日期篩選支援今日、今日之後與較舊，排序支援取餐/送達時間正反序，並用「新增外帶」進入點餐。點餐頁改為接近 iCHEF 出單畫面的左側票券、右側分類列與商品大方塊，新增外帶會立即建立可見佇列訂單，依序顯示 `No. 001`、`No. 002`，即使尚未加入品項也會保留在外帶/外送頁；內用訂單以 `樓層 1F`、`桌位 A1` 這類備註對應桌卡，樓層、當前樓層、桌位配置、容量、各桌人數、顯示偏好與候位清單會同步到 `floor_plan` runtime setting，localStorage 只做 API 失敗時的暫存快取。按「開啟訂單」時，可編輯的櫃台單會載回購物車；若狀態或收款讓它不能直接編輯，會切到訂單中心並展開該筆訂單供檢視。咖啡/茶飲品項會開啟選項面板並要求先選「飲品溫度」，未選時會顯示必填警告；票券上可直接修改顧客、方式、付款與常用備註；`order.scriptcoffee.com.tw` 會預設進入消費者線上點餐頁。
- 候位提前點餐：2026-05-11 對照 iCHEF 官方「現場候位登記與提前點餐」與實機 iCHEF POS 候位區，候位列新增「提前點餐 / 開啟點餐」；首次點擊會建立內用櫃台草稿單、帶入候位姓名/電話/人數/備註並把 `orderId` 寫回 `floor_plan.waitline`。候位等待期間可續編同一張草稿單；空桌入座時會走既有 `/orders/:id/floor` 更新樓層、桌位與人數，再載入原草稿單進點餐頁出單。`src/types/pos.ts`、`src/lib/posApi.ts` 與 `supabase/functions/pos-api/index.ts` 的 waitline normalizer 都保留 `orderId`，所以換平板、重開 App 或 fresh reinstall 後仍以 Supabase runtime + `orders.draft_lines` 還原，不把預點餐關聯留在 localStorage。
- 內用保留桌位：2026-05-12 以 Safari / Computer Use 對照 iCHEF 官方「內用保留桌位」，桌況頁新增空桌保留狀態。`floor_plan.tableHolds` 保存 table id、開始時間、到期時間與保留分鐘數，前台可選 60/120/180/240 分鐘或永久保留；桌卡以紅色顯示並倒數，到期時前台會移除 hold 並寫回 runtime，手動取消也會同步。`src/lib/posApi.ts` 與 `supabase/functions/pos-api/index.ts` normalizer 會過濾已過期或已刪除桌位的 hold，保留中的桌位不列入空桌、移桌或候位入座目標，也不能直接開桌。
- 櫃台效率操作：已有最近/常用品項快速加購並跨重啟保留、常用備註 chip、未送出櫃台訂單自動恢復、點外帶/外送頁櫃台訂單回購物車續編；購物車左欄改為高密度票券清單，服務方式保留直接切換，付款與「需要袋子」等常用備註同列顯示，備註 chip 可再次點按取消且已選時會用主色與勾選圖示回饋，多個品項註記改為可換行 chip，避免後段文字被單行截斷；品項壓縮為商品/註記、數量、出單狀態、金額四欄，可對單一明細切換 iCHEF 式「暫停出單」，該明細不會進列印 payload 但仍保留在金額與結帳。2026-05-12 依 iCHEF 官方「批次進行點餐清單中的商品操作」補上票券批次列，可依「出單」或「暫停」狀態計算符合明細，批次重印會先保存目前票券再以 line signature 篩選列印 payload，批次刪除則沿用 `deleteOrderItems` 操作驗證後移除符合狀態的明細。顧客資訊編輯會維持固定 POS 舞台高度，前端會在載入時記住鍵盤出現前的最大 layout viewport，高度變小的鍵盤 resize 事件不會更新工作台高度，避免左側票券被上推壓縮。商品分類列支援左右滑動快速切換分類，供應狀態為停售、售完或暫停供應的商品會保留在 POS 菜單中反灰、不可點按並顯示狀態標籤；連點工具箱 6 下進入後台編輯模式後，可長按拖曳分類 tab 或商品卡片交換順序，例如把咖啡與茶飲對調、把冰美式與熱拿鐵對調，拖曳與位移手勢會短暫抑制點擊，避免平板上長按排序被誤判為點餐。櫃台自己建立的外帶/外送單不再顯示鎖定逾時；外帶/外送訂單列改為單號/顧客資訊、直排狀態、右側金額三欄，讓「新單」等狀態與金額更接近現代出單票券。訂單搜尋與篩選狀態跨重啟保留、日期/取餐方式/來源/排序進階查詢與同列重設篩選、履約時間預警資料、訂單明細展開、訂單左滑揭露已完成/已取餐與刪除；佇列刪除不需額外驗證，外送/線上訂單按「已取餐」會直接歸納為已完成並從目前佇列消失。外帶/外送頁另有 iCHEF 式「快速出店」，依來源與完成時間批次處理已付款且到點的外帶/外送單，執行前需後台編輯模式，並會略過其他平板已鎖定的單；每筆仍走 `PATCH /orders/:id/status` 寫成 `served`，所以資料庫、Realtime、重開 App 與 fresh reinstall 都以同一狀態為準。另有列印單左滑刪除、庫存/低庫存提示、下單原子扣庫存、多平板訂單鎖定、收款確認、未收款訂單作廢、已收款退款沖銷、列印重印紀錄、正式開班/關班、現金臨時收支、單屏式關帳摘要、交班預檢、班別對帳、關帳異常檢查與強制確認、鍵盤捷徑；完成結帳或出單後會清空購物車、重置顧客電話/備註，內用單回到桌位地圖，外帶/外送單回到佇列，避免帶到下一張單。
- 影片 UI/UX 導入：2026-05-02 參考 iCHEF 操作影片後，保留現有預設色系，新增 POS 工具箱、外觀設定卡片、工具箱重新同步與常用工作區跳轉；外觀設定不再固定顯示在工具箱底部，點卡片後進入設定頁，可切換深色模式，並用 -200% 到 +200% 滑桿調整整體介面縮放、畫面密度、文字大小與浮動工具箱透明度。外觀設定會寫入 `pos_appearance` runtime setting，POS 載入 runtime 或收到 Realtime event 後自動套用，讓換平板或 fresh reinstall 後仍能沿用同一組外觀。POS 工作台現在由固定平板 large viewport 的縮放舞台承載，縮放會包含文字、浮動工具箱與供應狀態頁，並以不產生水平溢出為基準；浮動工具箱已提升為全域固定層，會在 POS、線上入口、後台與供應狀態等主要頁面維持可用，後台編輯模式下長按工具箱 2 秒可直接退出；Android APK 以 `adjustNothing` 搭配 `interactive-widget=overlays-content` 避免鍵盤 resize WebView。供應狀態入口已常駐在列印/前台操作頁，不再只限 Native APK 顯示，且支援搜尋、分類、狀態篩選、批次暫停/恢復篩選結果、儲存確認與回復上一個動作；2026-05-04 供應狀態工具箱擴成商品/分類/註記管理，可新增/刪除分類與商品、建立註記大項與選項、把任意註記綁定到任意商品，並可在註記群組建立後切換必選或非必選，儲存時會同步分類順序、註記群組、商品綁定與註記停售狀態到 `online_ordering` runtime，且只有成功寫入資料庫才清除未儲存狀態，供新平板與 `order.scriptcoffee.com.tw` 透過 Realtime 或 fallback polling 更新。後台已新增營運紀錄時間線，整合平板心跳、開關班與關鍵稽核事件，可搜尋、依類型/平板篩選並匯出 CSV；線上點餐設定已接 `online_ordering` runtime setting，可控制接單、預約、平均備餐、未確認提醒、接單確認、提示音播放模式與音量。2026-05-03 依 iPad 截圖重塑主流程，主佇列第一層先收斂為搜尋/條件/訂單列，佇列顯示短單號且完整單號保留在 title，鎖定標籤不顯示 `tablet-*` 類平板識別，深色模式需維持標題與顧客文字可讀；2026-05-04 依新增外帶影片調整為進入點餐時立即建立可見草稿單號，送出後沿用同一單號；本輪再擴成先建單後編輯，空單與未結帳單會先寫入 Supabase `orders.draft_lines`，正式結帳/出單後透過 `finalize_pos_order()` 轉正式明細並扣庫存，只有 API 失敗時才降級到本機草稿或 `script-coffee-pos-pending-orders` 補同步；2026-05-08 直接用目前開啟的 iCHEF app 對照內用桌位、候位、顯示設定、通知中心、外帶/外送、交易查詢、工具箱與供應狀態，新增 `floor_plan` runtime setting、`/orders/:id/floor` 移桌 API、候位電話/備註表單、桌位容量設定與外帶日期/排序對照；同日依平板截圖修正「開啟訂單」無法檢視的情境，並將 `floor_plan` 擴充為樓層、當前樓層與每桌 `floorId`，後台編輯模式可直接管理樓層與桌位地圖；任務快篩、履約提醒與 SOP 資料後續適合放進工具箱延伸；班別關帳已改為摘要橫列、交班預檢與右側班別對帳的單屏優先布局，未交付、待收款、付款異常、列印失敗與作廢記錄仍可直接帶入外帶/外送頁篩選處理。
- 前端資料流：`src/lib/posApi.ts` 是唯一正式 POS API client，負責把 Supabase Edge Function snake_case 回應轉成 Vue view model；`src/lib/posRealtime.ts` 只訂閱低敏感 `pos_realtime_events` invalidation stream；`usePosSession()` 收到 Realtime event 後仍回到 `pos-api` 重新拉取訂單、runtime、班別或商品資料，並保留既有 polling 與本機 fallback。
- 門市 SOP 助手：`src/data/posKnowledge.ts` 保存本機 SOP 條目與分類，`src/App.vue` 只處理搜尋、篩選、選取與跳轉；未來真實 SOP 匯入應優先擴充資料檔。
- 後台入口：`src/components/AdminPanel.vue` 管理商品菜單、線上點餐 runtime 設定、庫存數量、低庫存門檻、暫停供應至、預約單可售、POS/線上/掃碼可見性、備餐站、會員錢包與 CSV 匯出、營運日報與 CSV 匯出、金流回呼事件篩選/匯出、出單機規則、角色權限、員工識別碼、POS 操作驗證開關、員工打卡紀錄、營運紀錄時間線、平板在線與操作稽核匯出；前端需先連點工具箱 6 下進入後台編輯模式，後端管理端點不再要求舊驗證 header。
- iCHEF 補齊：2026-05-09 依 1-8 缺口補上 CRM、標籤、服務費/其他費用、優惠券/點數折抵、訂位週/月管理資料、線上推薦/AI/多語設定、硬體外設設定與更細的商品供應規則。2026-05-10 參照 iCHEF 官方「線上訂位黑名單」補上 `reservation_blacklist_entries`，後台「iCHEF 補齊」頁可用手機號碼新增/解除黑名單，也可從訂位列直接加入或解除；新增訂位時若手機命中黑名單會先提示原因，店員再次按建立才會覆蓋建立。前台付款頁可查會員、套顧客類型/點數/券、選訂單標籤、調整服務費/其他費用/折抵與推薦加購；後台集中管理標籤、券、多結帳口帳本、店面流程/外帶設定、訂位、訂位黑名單、推薦、供應時段、跨日預約可售、線上營業前供應檢查、藍牙掃碼器、行動支付掃碼、錢櫃與指定 iPad 列印 QR code。2026-05-12 對照 iCHEF 官方「標籤與備註」後，工具箱標籤管理支援新增、改名、調色、排序與刪除，並落實名稱 15 字、最多 30 個、不可空白或同名；`engagement_settings.orderLabels` 的前端與 Edge Function normalizer 都套用同樣限制，點餐頁標籤顯示順序跟隨設定順序。2026-05-12 服務費改為 `engagement_settings.serviceCharge` runtime：可設定啟用、顯示名稱、內用/外帶/外送費率、折扣前/折扣後計算，以及不計算分類/品項；POS 與線上/QR 送單都用同一份設定，公開建單時 `pos-api` 會重新計算。2026-05-12 直接觀察 iCHEF 後台「店面管理 > 出單機設定」與官方出單機知識庫後，`printer_settings.rules[]` 新增 `timings`，可分別控制「出單」與「重印」是否列印；前台列印站與後台出單規則都可調整，`buildOrderPrintPlan()` 依時機拆 print jobs，舊 runtime 由 `20260512101000_add_print_rule_timings.sql` 與 API normalizer 補成兩者啟用。2026-05-12 多結帳口帳本改由 `engagement_settings.checkoutCounters` 管理，配合 `register_sessions.book_id` 與 `orders.register_session_id` 分開統計各 iPad 結帳口。2026-05-12 店面流程與外帶設定改由 `engagement_settings.workflowAlerts` 管理，POS 依開始/結束時間切分訂單查詢，並套用出餐確認、預約取餐提前提醒、候位/未出單/出餐等待/店內滯留/外帶等待警示、外帶預設取餐、循環開單與自動出店；出餐確認預設關閉，內用自動出店預設關閉，外帶/外送自動出店預設開啟，各流程警示預設關閉，內用掃碼已結帳訂單仍需手動出店。資料會走 `pos-api` 與 Supabase，localStorage 只保留草稿/fallback。
- iCHEF 訂單 QR Code 規則：2026-05-12 直接觀察 iCHEF 後台「訂單 QR Code 規則」與官方內用掃碼點餐說明後，`online_ordering.sessionQrCode` 新增 `autoPrint`、`stationId` 與 `logoText`。POS 保留訂單列/訂單明細手動列印 QR；若後台開啟自動列印，建立內用桌位草稿後會用指定出單機建立 QR `print_jobs` 並送 Android LAN printer，QR URL 繼續帶 `source=qr`、訂單與桌號參數。舊 runtime 由 `20260512112000_add_session_qr_auto_print.sql` 補成預設手動列印。
- iCHEF 用餐與點餐限時：2026-05-12 對照 iCHEF 官方知識庫「用餐與點餐限時｜內用掃碼點餐」與後台 `雲端餐廳 > 內用掃碼點餐 > 用餐與點餐限時`，`online_ordering.dineInTimeLimit` 新增 `enabled`、`mealMinutes`、`lastOrderBeforeEndMinutes` 與 `holidayRules`。後台「線上點餐」可設定預設規則與假日規則；POS 工具箱卡片只切換功能狀態並寫回同一份 runtime，對齊 iCHEF 後台/POS 開關同步。`src/lib/dineInTimeLimit.ts` 是前端共用計算器，桌位卡用訂單 `createdAt` 顯示剩餘/逾時與最後加點標示，用餐結束後桌卡加鬧鐘警示；`src/lib/printing.ts` 的訂單 QR URL 會帶 `openedAt`，消費者 QR 頁讀 `openedAt` / `startedAt` 顯示最後加點與用餐結束時間。`pos-api` 會在 `POST /orders` 對 `source=qr` 且 `serviceMode=dine-in` 的送單用同一份 runtime 重算最後加點，逾時回 409，不只靠前端畫面。舊 runtime 由 `20260512123000_add_dine_in_time_limit_settings.sql` 補成預設關閉、用餐 120 分、最後加點 0 分。
- iCHEF 內用掃碼結帳流程：2026-05-12 用 Safari 登入 iCHEF 後台 `雲端餐廳 > 內用掃碼點餐 > 結帳流程`，確認欄位為「結帳模式」且選項是「先結」與「後結」，結帳說明上限 500 字，同頁保留統編與載具欄位設定。`online_ordering.dineInCheckout` 新增 `mode`，預設 `postpaid`；後台「線上點餐」可切換先結/後結並保存到 Supabase runtime。QR 內用頁在先結模式只顯示 LINE Pay、街口、線上刷卡等線上付款方式；後結模式會隱藏付款選項，送單後以現場待收款進 POS。`pos-api` 在 `POST /orders` 重新讀 runtime：先結 QR 內用若不是線上付款會回 409，後結 QR 內用會覆寫為 `paymentMethod='cash'`、`paymentStatus='pending'`，不靠 localStorage。舊 runtime 由 `20260512133000_add_dine_in_checkout_settings.sql` 補成後結。
- iCHEF 支付模組設定：2026-05-11 用 Safari 觀察 iCHEF 後台 `雲端餐廳 > 內用掃碼點餐 > 支付模組設定`，確認支付模組包含「現場付款」「線上刷卡」與「91APP 支付線上刷卡」。`PaymentMethod` 新增 `app91-card`，`online_ordering.paymentMethods` 預設追加停用的「91APP 支付線上刷卡」模組；消費者頁會把它視為線上付款，可用於外送與先結 QR 內用，POS 顯示、列印、混合支付、櫃台草稿/還原 sanitizer 與 `pos-api` 驗證都接受同一 enum。`20260512170000_add_91app_online_payment_method.sql` 會替 `public.pos_payment_method` enum 與舊 `online_ordering.paymentMethods` 補上此方法。
- iCHEF 基本支付自定義記錄：2026-05-12 用 Safari / Computer Use 讀取官方「多元支付整合：同台 POS 搞定」與「[多元支付] 自定義支付」，確認基本支付記錄有現金、信用卡與可用於禮券/兌換券的自定義支付，且結帳時可填支付備註供營業報表的結帳/作廢紀錄查詢。`PaymentMethod` 新增 `custom`，`20260512260000_add_custom_payment_method.sql` 替 `public.pos_payment_method` enum 補值；`20260512270000_add_order_payment_note.sql` 新增 `orders.payment_note`。POS 付款頁與票券快捷顯示「刷卡」與「自定義支付」，付款頁可填支付備註；拆單、混合支付、列印與營運日報保存同一付款 enum，交易查詢、`src/lib/posApi.ts` 與 `pos-api` normalizer 另保存並回傳 payment note。`card` 用於未整合刷卡機的信用卡記錄；`custom` 屬於現場基本支付，保持在待收款流程，不加入 `online_ordering.paymentMethods` 預設與外送/QR 先結線上支付白名單。
- iCHEF 備註欄位設定：2026-05-12 用 Safari 登入 iCHEF 後台 `雲端餐廳 > 內用掃碼點餐 > 備註欄位`，並對照官方知識庫 `備註欄位設定｜雲端餐廳`，確認餐點備註可隱藏/顯示，訂單備註可隱藏、必填或選填並可設定提示文字。`online_ordering.commentFields` 新增 `itemNotes`、`orderNote`、`orderNotePlaceholder`，預設顯示餐點備註、訂單備註選填。消費者頁會依 runtime 隱藏/顯示餐點文字註記與訂單備註，必填時前端阻擋；餐點備註保存到品項 `options`，訂單備註保存到 `orders.note`，QR 桌號資訊仍保留在同一欄。`pos-api` 會重新讀 runtime：訂單備註必填時缺內容回 409，隱藏訂單備註時只保留桌位/樓層等系統資訊，隱藏餐點備註時過濾 `文字註記：` options。舊 runtime 由 `20260512143000_add_online_comment_field_settings.sql` 補預設值。
- iCHEF 桌位 QR Code：2026-05-12 用 Safari 登入 iCHEF 後台 `雲端餐廳 > 內用掃碼點餐 > 桌位 QR Code`，確認功能包含上傳 90x90 JPG/PNG Logo、選擇經典黑色/蘋果綠色/夕陽橘色/檸檬黃色/薰衣草紫色、下載全部與單桌下載，且桌卡步驟會依先結/後結顯示不同說明。`online_ordering.tableQrCode` 新增 `theme`、`logoText`、`logoDataUrl`，後台「線上點餐」可設定桌卡樣式並依 `floor_plan` 下載所有樓層桌位或單桌 A4 HTML 桌卡；`src/lib/tableQrCards.ts` 用 `qrcode` 產生實際 QR 圖，URL 會帶 `source=qr`、`mode=dine-in`、`floor` 與 `table`。`ConsumerOrderPage` 讀 `floor` 後會在頁首與送單備註保留樓層/桌號，`src/lib/printing.ts` 的訂單 QR URL 也會補帶樓層。舊 runtime 由 `20260512153000_add_table_qr_code_settings.sql` 補預設值，不依賴 localStorage。
- iCHEF 店家營業資訊：2026-05-11 用 Safari 觀察 iCHEF 後台 `雲端餐廳 > 內用掃碼點餐 > 店家營業資訊`，並對照官方知識庫 `店家營業資訊｜內用掃碼點餐`，確認欄位包含店家名稱、聯絡電話、店家地址、營業人資訊提示、提醒事項、顯示全文與最多 4 張店家封面圖片；多張封面會在內用掃碼網站輪播，提醒事項預設顯示 3 行。`online_ordering.storeProfile` 新增 `name`、`phone`、`address`、`notice`、`noticeExpanded` 與 `coverImageDataUrls`。後台「線上點餐」可維護這些欄位並保存到 Supabase runtime；消費者線上/QR 頁首會顯示店名、電話、地址與提醒事項，封面圖片多張時每 5 秒輪播。`src/lib/posApi.ts` 與 `supabase/functions/pos-api/index.ts` 會正規化名稱 60 字、電話 32 字、地址 160 字、提醒事項 3000 字與最多 4 張 data URL 圖片；重開 App、換平板或 fresh reinstall 後仍由 `/settings/runtime` 還原，不放 localStorage。
- 套餐商品：2026-05-11 對照 iCHEF 官方「什麼是 iCHEF 商品」「套餐子項目多選及重覆選擇」「設定出單機列印的商品」補上套餐商品設定。供應狀態商品編輯區新增「套餐子項目」，可設定必選/選填、最少/最多份數、是否允許重複選擇、可選單點商品與加價；設定保存於 `online_ordering.comboProductAssignments`，由 `src/App.vue`、`ConsumerOrderPage.vue`、`src/lib/posApi.ts` 與 `supabase/functions/pos-api/index.ts` 正規化。POS 與線上點餐共用套餐選擇面板，未達必選會阻擋加入，選到的子商品會出現在票券註記與訂單明細。2026-05-11 接續對照 iCHEF「商品加減價與註記」的套餐編輯方式，當套餐子商品本身綁有註記群組時，同一面板會在該子商品底下顯示子商品註記、驗證必選/最多選、計入加價，並把 `comboItems[].options` 保存到草稿與正式訂單。2026-05-11 再補 iCHEF 同篇文件的臨時「文字註記」與 `+ 加價` / `- 減價`；一般商品加入票券後也可點品項名稱回到選項面板，文字註記存入 `CartLine.options`，加減價反映在 `unit_price`，並沿用 `useVariablePriceNotes` 員工識別碼驗證。`20260512070000_add_combo_product_runtime_settings.sql` 新增 `order_items.combo_items`，正式 `create_pos_order()` / `finalize_pos_order()` 會保存子商品、扣子商品可售庫存並套用子商品自動耗料；`20260512083000_add_combo_child_option_consumption.sql` 會在 `order_items` insert 後只針對 `combo_items[].options` 套用 option 類自動耗料，避免重複扣子商品本身；`src/lib/printing.ts` 的列印規則會用 `CartLine.comboItems` 讓列印站可依套餐內品項命中或排除，對齊 iCHEF「包含所有套餐內的此品項」邏輯。資料來源是 Supabase runtime/API，localStorage 只保留既有 fallback 快取。
- 付款拆單：2026-05-11 對照 iCHEF 官方知識庫「拆單各付各或均分」與「折扣頁」的拆單說明，補上點餐頁「付款/拆單」入口、付款頁均分/新增子單/重置、商品各付各指派、每張子單付款方式與已結/未結狀態。子單狀態寫入 `orders.payment_splits`，由 `20260511202000_add_order_payment_splits.sql`、`src/lib/posApi.ts`、`usePosSession()`、`pos-api` create/draft/finalize payload 與訂單中心摘要共同讀寫；換平板、App 重開與 fresh reinstall 都應以 Supabase 訂單欄位還原。雙平板 smoke 已加入平板 A 對候位草稿單拆成 2 張子單並標記一張已結，再確認平板 B 外帶/外送訂單中心看到同一拆單摘要。
- 混合支付與交易明細：2026-05-11 續對照 iCHEF 官方「同張訂單使用多種支付模組」與「列印多張交易明細」，付款頁新增混合支付區，可新增多筆付款、逐筆輸入金額/方式/已結狀態，並阻擋金額不等於訂單合計或同張單使用多個線上支付模組。`orders.payment_breakdown` 保存付款分配，`orders.transaction_receipt_count` 保存結帳時自動列印交易明細張數；訂單中心會顯示混合支付摘要並可事後補印「交易明細」。關帳付款方式對帳已改讀 `payment_breakdown` 分攤金額，包含自定義支付；沒有混合支付時才回到 `orders.payment_method`。
- 優惠活動 / 折扣模組：2026-05-12 對照 iCHEF 官方「優惠活動」新增店家優惠、排序與外帶/外送活動標籤流程。新增 `discount_settings` runtime setting 與 `src/lib/discounts.ts` 共用計算器；後台「優惠活動」可管理自動/手動、全單/分類/指定商品、折扣/折讓、最低消費、POS/線上通路、POS 自動套用、服務方式、優惠時間與操作驗證。POS 付款頁會顯示可用優惠活動、允許手動套用或停用自動優惠，若該活動要求驗證則併入 `applyManualDiscounts` 員工識別碼流程。消費者線上點餐會套用線上啟用的自動優惠，且用扣除優惠後金額計算外送最低金額與滿額免運；`pos-api` 在 `POST /orders`、草稿與 finalize 也會載入 runtime 重新計算自動優惠，避免只靠前端或 localStorage。
- 會員優惠券兌換狀態：2026-05-12 對照 iCHEF 官方「優惠券」規則補上跨平板券狀態。iCHEF 規則包含每張優惠券同一會員僅能使用 1 次、每筆交易只能選 1 張券、完成使用會列入已使用紀錄、作廢使用優惠券的訂單時會把已使用券退回會員帳戶。本專案新增 `20260512024500_add_coupon_redemption_state.sql`，讓 `member_coupons` 保存 `redeemed_order_id`、`redeemed_at` 與 `redemption_station_id`。`pos-api` 在 `POST /orders` 與 `POST /orders/:id/finalize` 會先用 `status='active'` 條件更新 claim 優惠券，成功建單後綁定訂單；若券已被另一台平板使用、過期或會員不符，API 回 409 並讓 POS 移除本機暫存單，不寫入 pending local order。`POST /orders/:id/void` 與 `POST /orders/:id/refund` 會把該訂單已使用券退回 active，並寫入 `member.coupon.restore` 與訂單稽核 metadata。後台券列表會顯示可使用/已使用/已過期、綁定會員、使用時間、兌換訂單與站台。
- 會員點數活動：2026-05-12 對照 iCHEF 官方「優惠與點數活動」補上點數規則與流水。`engagement_settings.loyaltyPoints` 保存啟用狀態、累點/折抵開關、消費金額換 1 點、最低折抵點數與單筆折抵上限；後台「iCHEF 補齊」可直接維護，POS 付款頁會依會員餘額、訂單金額與 runtime 即時計算可折抵點數與本單可得點數。`20260512180000_add_member_points_ledger.sql` 新增 `member_points_ledger`、no-direct-client RLS 與 `redeem_pos_member_points()`、`release_pos_member_point_redemption()`、`finalize_pos_member_points()`、`restore_pos_member_points_for_order()` service-role RPC。`pos-api` 在正式建單與草稿 finalize 前先原子扣點，建單失敗會釋放扣點，建單成功後把折抵流水綁到訂單並寫入累點流水；未收款作廢與已收款退款會依尚未反轉的點數流水回補折抵點並反轉本單累點，避免舊訂單只有欄位沒有流水時錯誤加點。
- 目前營業概況 / 系統資訊：2026-05-11 依 iCHEF 工具箱第二頁與官方「目前營業概況」說明補上工具箱面板；2026-05-12 再依官方「進入此頁面時須經過權限驗證」補上 `viewCurrentSales` 權限。`current-sales` 入口會先要求員工識別碼並呼叫 `/access/verify`，舊角色若已有 `manageReports` 或 `closeRegister` 會由 normalizer 自動補上 `viewCurrentSales` 以維持相容；通過後以開班時間作為餐期起點，顯示未結帳金額、已結帳金額、餐期訂單、付款異常與服務方式拆分；未開班時以今日 00:00 起的訂單估算。`system-info` 集中顯示平台、POS API、工作站心跳、線上接單、桌位圖、列印站、商品供應與班別資訊，並提供重新同步。這兩個面板只讀取既有 API/runtime/register/order 狀態，不新增資料表或 localStorage。
- 工具箱標籤管理：2026-05-11 依 iCHEF POS 工具箱「標籤管理」補上店內常用服務標籤面板。面板直接編輯 `engagement_settings.orderLabels`，儲存走 `PATCH /admin/settings/engagement_settings`，點餐頁標籤列與訂單送出後的 `orders.order_labels` 都沿用同一組設定與同一份資料庫來源。
- 工具箱裝置管理：2026-05-11 直接觀察 iCHEF POS「裝置管理」後補上面板。出單機讀 `printer_settings.stations`，刷卡/掃碼/錢櫃外設讀 `engagement_settings.hardwareDevices`，未印出單據從目前載入訂單的 `print_jobs` 彙整；「重新整理連線狀態」重拉後端資料，「取消所有未印出的單據」在後台編輯模式下逐筆呼叫既有列印單刪除流程。
- 工具箱顧客資訊管理：2026-05-11 直接觀察 iCHEF POS「顧客資訊」後補上面板。搜尋、顧客類型篩選、消費時間/建立時間排序與新增顧客資訊都走既有 `/admin/members`、`members` 與會員流水資料；最近消費時間以 `transaction_ledger` 最新紀錄估算，無流水時退回 `members.updated_at`。
- 工具箱庫存管理：2026-05-11 直接觀察 iCHEF POS「庫存狀態」後補上面板。iCHEF App 會顯示搜尋庫存品項、空狀態「請先在 Web 設定中建立庫存品項」與 `+` 選單的進貨、退貨、消耗、報廢、盤點；本專案把同一流程放進工具箱並可在平板端建立庫存類別/品項。`20260511235500_add_inventory_management.sql` 新增 `inventory_categories`、`inventory_items`、`inventory_records` 與 `apply_inventory_record()` RPC，`pos-api` 新增 `/admin/inventory`、`/admin/inventory/categories`、`/admin/inventory/items`、`/admin/inventory/records`，每筆操作會原子更新庫存、寫入操作紀錄與 `inventory.record.*` 稽核，並用 `inventory_management` realtime topic 通知其他平板重拉。後續依官方「商品／單一註記自動消耗庫存品項」補上 `20260511235900_add_inventory_auto_consumption.sql` 與 `inventory_consumption_rules`，後台商品菜單可為商品或註記設定每份消耗量，正式建單/出單時會寫入 `inventory_records.consumption` 並扣 `inventory_items.stock_quantity`，但 `printPaused=true` 的暫停出單品項不會扣自動消耗庫存。庫存管理資料不使用 localStorage 作為來源。
- 交易查詢與作廢：2026-05-11 直接用 iCHEF POS 工具箱觀察「交易查詢與作廢」後補上 `transactions` toolbox panel。查詢條件包含發票/收據號碼、載具/捐贈碼、桌號與訂單號碼；結果列顯示時間、收據標籤、桌號、訂單號與結帳金額，點選後顯示交易預覽、付款/統編/載具與品項明細。補印走 `printTransactionDetail()`，作廢/退款走既有 `voidOrderForStation()` / `refundOrderForStation()` 與後台編輯模式，不新增資料表或 localStorage。
- 錢櫃管理：2026-05-11 直接用 iCHEF POS 工具箱觀察「錢櫃管理」後補上 `cash-drawer` toolbox panel。面板分為「錢櫃」與「臨時收支」，會顯示 `engagement_settings.hardwareDevices` 的錢櫃裝置、目標列印站、最近錢櫃開啟紀錄與本班 `register_cash_adjustments`。`src/lib/printing.ts` 新增 ESC/POS cash drawer pulse payload；`usePosSession.openCashDrawerForStation()` 在 Android APK 用既有 `LanPrinter` plugin 送 pulse，Web fallback 則只寫預覽。`pos-api` 新增 `GET /cash-drawer/events` 與 `POST /cash-drawer/open`，每次開啟都寫入 `pos_audit_events.action='cash_drawer.open'`，`20260511223000_add_cash_drawer_realtime_topic.sql` 擴充 `pos_realtime_events.topic` 為可接 `cash_drawer`，其他平板收到後重拉同一份紀錄；不新增 localStorage。同日再依 iCHEF 官方「錢櫃的開啟設定」補上支付模組錢櫃開關：`online_ordering.paymentMethods[].opensCashDrawer` 會決定收款/結帳完成後是否自動開錢櫃，現金預設開啟，其他付款預設關閉。
- iCHEF 官方知識庫對照：2026-05-10 以 `support.ichefpos.com` 的 iCHEF 後台分類作為缺口清單，先補帳務管理的「現金臨時收支」，再補員工帳號/權限與員工打卡第一版。2026-05-11 對照官方「外帶 / 外送訂餐流程」補上結帳說明、統一編號欄位與載具條碼欄位顯示設定；後台線上點餐設定會把欄位開關寫入 `online_ordering` runtime，消費者頁送單後把統編/載具寫入 `orders`，POS 訂單明細、線上新單提醒與列印 payload 都讀同一份資料。同日再依「臨時調整營業狀態」與外送/掃碼訂單處理方向補上自取、內用掃碼與外送的服務方式開關，前端停用已關閉模式，API 也會拒絕被關閉的 service mode；並補上 iCHEF 支付模組設定方向，讓取餐時付款、LINE Pay、街口、線上刷卡、91APP 支付線上刷卡與轉帳可在後台啟用、改顯示名稱與調整排序，消費者頁與 API 都讀 `online_ordering.paymentMethods`。同日再依官方「提供外送服務」補上外送費、外送最低金額、滿額免運與預計車程，消費者外送只保留線上付款，API 會用扣除折抵後金額計算門檻與 `extra_fee_amount`。同日再依官方「提供預約訂單服務」補上取餐時間間隔、最長可預約天數與多段星期/時間窗，前端與 API 都會用 `online_ordering` runtime 驗證希望時間；並依官方「外帶/外送區已結帳訂單快速出店」補上批次出店，篩選來源與完成時間後把已付款且到點的外帶/外送訂單逐筆更新為 `served`。2026-05-12 再依官方「優惠活動」補上自動/手動店家優惠、排序、POS/雲端餐廳通路與線上活動折扣，所有規則存在 `discount_settings` runtime。關帳頁班別面板可登記收入/支出金額、原因、備註與站台，常用原因包含備用金、找零補入、零用金支出、食材採買與外送平台現金；新增後即時重算預期現金並顯示最近 60 筆，後台 SOP 搜尋新增「現金臨時收支」條目。`access_control` runtime setting 現在同時保存角色與員工識別碼，工具箱「員工打卡」呼叫 `POST /time-clock`，後端依同一員工最新 `staff_time_clock_entries` 自動切換上班/下班並寫入 `employee.time_clock` 稽核。2026-05-12 依官方「小結／關帳報表與寄送關帳信」補上 `sendDailyReports` 與員工 `reportEmail`；關帳後 `POST /register/close` 會建立 `closeout_report_deliveries` shared outbox，若 Edge Function 設定 `RESEND_API_KEY` 與 `POS_REPORT_EMAIL_FROM` 則即時寄送，否則保留 queued 紀錄供後台跨平板查看。同日依官方「[帳務管理] 重印紙本小結、關帳與銷售紀錄」補上關帳頁「瀏覽紀錄 / 補印」，由 `/register/sessions` 依目前多結帳口帳本載入最近班別，並以同一份班別快照補印小結、關帳與銷售紀錄。
- POS 操作權限驗證：2026-05-11 直接觀察 iCHEF 後台「帳號與權限」的開單、出單至廚房、刪單、刪品項、使用變價註記、結帳、服務費、手動折扣、雲端餐廳接單/取消與帳務權限後，將 `access_control` 擴充為角色權限、員工識別碼與 `protectedPermissions`。`/settings/runtime` 只回傳低敏感 `accessPolicy.protectedPermissions` 給平板；前台在開單、出單、轉單、刪單/取消線上單、刪品項、變價註記、結帳/收款、服務費/其他費用、手動折扣/點數/優惠券、線上接單/拒絕、作廢、退款、錢櫃與臨時收支等被保護操作前跳員工識別碼視窗，並呼叫 `POST /access/verify` 由 `pos-api` 驗證啟用員工與角色權限。驗證成功會寫入 `pos_audit_events.action='access.verify'`；關帳 API 也改用同一 helper 檢查 `closeRegister` 權限。2026-05-12 角色權限再加入 `sendDailyReports`，後台員工列只在該角色有此權限時顯示「報表寄送 Email」，對齊 iCHEF 未勾權限就不出現欄位的行為；同日再依官方「權限紀錄」入口，讓後台「權限」頁用 `/admin/audit-events?action=access.verify` 讀專用權限紀錄，顯示員工、角色、驗證權限、平板與時間，並可下載 CSV。這些設定存在 Supabase `pos_settings.access_control` 與既有 `pos_audit_events`，換平板、重開 App 或 fresh reinstall 後仍同步，不使用 localStorage 保存權限狀態。
- iCHEF 打卡紀錄報表：2026-05-12 以 Safari / Computer Use 讀取官方「使用員工打卡功能」與「打卡紀錄」，確認 POS 端 `工具箱 > 打卡` 可明確點「上班」或「下班」，並用「瀏覽記錄」查看最近 7 天與「所有員工」篩選；後台可用時間區間、員工篩選並下載打卡紀錄，且官方建議區間不超過 93 天。本專案沿用既有 `staff_time_clock_entries`，POS 端新增 `GET /time-clock` 讀最近 7 天並支援 `staffAccountId` 篩選，`POST /time-clock` 可接 `eventType=clock-in|clock-out`，未帶 eventType 時仍保留舊的自動切換行為；`/admin/time-clock` 則可接 `startDate`、`endDate`、`staffAccountId` 與 `limit`，超過 93 天回 400。工具箱「員工打卡」面板會載入最近紀錄、提供全體/指定員工下拉與刷新，後台「權限」頁的打卡紀錄負責長區間查詢與 CSV 下載，不新增資料表或 localStorage。
- 交接提醒：佇列刪除、線上取消與品項刪除是否需要員工識別碼，現在以 `protectedPermissions` 為準；舊版文件中「不需額外驗證」只代表未啟用操作驗證開關時的快速路徑。
- 品牌素材：`public/assets/script-coffee-logo.png` 來自本機 `SC/logo.png`。
- GitHub repo：`scriptcoffeeshop/pos`，目前為 public。
- Git remote：`git@github-scriptcoffeeshop:scriptcoffeeshop/pos.git`。
- SSH 綁定：repo-local `core.sshCommand=ssh -i ~/.ssh/id_ed25519 -o IdentitiesOnly=yes`。
- 第一次 CI：GitHub Actions run `25003328938`，`verify` job 已通過；Pages deploy job 後續已改為 `main` push 自動部署。
- 後端決策：POS 使用獨立 Supabase 專案 `uuzwcmceotooocyrtnao`，不沿用咖啡訂購專案的資料庫。
- 本機 env：`.env.local` 與 `.env.supabase.local` 已建立並被 `.gitignore` 保護，不提交真實值。
- GitHub Secrets / Variables：已設定 Supabase deploy 需要的 secrets 與前端 build variables。
- Pages 網域：`order.scriptcoffee.com.tw` 已綁定 GitHub Pages；2026-04-28 使用者回報 HTTPS 已完成。
- 遠端部署：`20260427155000_initial_pos_schema.sql` 已推到 Supabase；`pos-api` Edge Function 已部署並通過 `/health`、`/products` 驗證。
- POS API 同步：商品、訂單、runtime 出單機/線上點餐/外觀設定/桌位圖與收銀班別會從 `/products?channel=pos`、`/orders`、`/settings/runtime`、`/register/current` 載入；庫存管理會從 `/admin/inventory` 讀取類別、品項、最近操作紀錄與自動消耗規則，並透過 `/admin/inventory/records` 走資料庫 RPC 原子更新存量，透過 `/admin/inventory/consumption-rules` 管理商品/註記消耗設定。POS 通路商品同步以 `pos_visible` 為列表邊界，不再用 `is_available=true` 過濾，讓已寫入資料庫的停售商品仍留在 POS 與供應狀態中反灰顯示並可恢復，線上/QR 通路仍只回傳可售商品。外觀設定寫進 `pos_appearance` runtime setting，包含 `interfaceScale`、`densityScale`、`textSize`、`darkMode` 與 `toolboxOpacity`。桌位圖寫進 `floor_plan` runtime setting，包含樓層、當前樓層、每桌 `floorId`、桌位座標/容量、顯示偏好、各桌人數與候位清單；`20260508103000_add_floor_levels_to_floor_plan.sql` 會把既有無樓層的桌位補到 `1F`，API normalizer 也保留同樣的向後相容。移桌走 `PATCH /orders/:id/floor`，會更新訂單為內用、重寫樓層/桌位/人數備註並寫入 `order.floor.move` audit event。商品分類已由固定 enum 放寬為 `products.category text`，工具箱的分類順序、註記大項/選項、商品註記綁定與註記供應狀態會寫進 `online_ordering.menuCategories`、`menuOptionGroups`、`productOptionAssignments` 與 `noteSupplyStatuses`，商品新增/刪除/供應狀態變更與商品拖曳排序走 `/admin/products`、`/admin/products/:id` 寫資料庫與稽核，不再把新增商品或排序調整視為本機完成；商品供應狀態由 `products.is_available`、`online_visible`、`qr_visible` 與庫存/暫停欄位推導，分類拖曳寫回 runtime、商品拖曳寫回 `products.sort_order` 後，POS 會重新拉取後端資料校準畫面。
- POS Realtime 同步：`20260506110000_add_pos_realtime_events.sql` 新增 `pos_realtime_events`，由 `orders`、`pos_settings`、`register_sessions` 與 `products` trigger 寫入低敏感 topic/entity payload，並加入 Supabase Realtime publication。POS 工作台訂閱 `orders`、`runtime_settings`、`register_sessions`、`products`、`online_order_reminders`、`cash_drawer` 與 `inventory_management` topic；收到事件後 debounce 重拉 `/orders`、`/settings/runtime`、`/register/current`、`/products`、`/online-order-reminders/state`、`/cash-drawer/events` 或目前開啟的 `/admin/inventory` 面板，讓多平板 claim lease、線上接單提醒、錢櫃開啟紀錄、庫存管理、狀態更新、外觀與供應設定變更不用等下一輪 polling。若正在建單、出單、claim、收款、作廢或退款，訂單同步會延後重試；Realtime `CHANNEL_ERROR`、`TIMED_OUT` 或 `CLOSED` 時會以 2 到 30 秒退避重連，20 秒 POS polling、前景補同步與 30 秒 heartbeat 保留。2026-05-07 起，Android APK 會註冊 `OnlineOrderNotifier` native plugin；WebView 在前景仍使用既有 Realtime/提示音/接單提醒，桌況頁只顯示單一待接單橫幅，其他工作頁才顯示浮動提醒；「查看訂單內容」會開啟顧客、付款、地址、品項與備註明細，店員可直接接單或拒絕接單。背景或螢幕熄滅後由 `OnlineOrderPollingService` 以前景服務執行，依 `online_ordering.acceptanceRequired`、`unconfirmedReminderMinutes`、`notificationRepeatMode`、`soundEnabled` 與 `notificationVolume` 短輪詢 `/settings/runtime`、`/orders` 與 `/online-order-reminders/state`，用 Android notification 與原生 tone 提醒線上/掃碼新單；「稍後提醒」會把目前 active order ids 傳給 native snooze，「接單」、拒絕接單或訂單消失會 mark seen，避免回前景後重複叫。Web 端背景 fallback 只在 Browser Notification API 已授權時顯示通知，未授權時不阻塞原有前景提醒。
- 線上提醒共享狀態：`20260509023000_add_online_order_reminder_states.sql` 新增 `online_order_reminder_states`，並把 `pos_realtime_events.topic` 擴充為可接 `online_order_reminders`。`pos-api` 新增 `GET/PATCH/POST /online-order-reminders/state`，前端 `src/lib/posApi.ts` 會以訂單編號查詢或寫入 `snooze`、`seen`、`accepted`、`rejected`；`usePosSession()` 在套用遠端訂單前先同步 reminder states，並在收到 `online_order_reminders` event 時只重拉提醒狀態。API 已設定時，APK 啟動、fresh reinstall 後首次載入與從背景回前景都會先把 reminder state 視為未水合，直到 `/online-order-reminders/state` 成功套用後才恢復前景提示音、Web fallback 與 native notifier active orders，避免用本機舊狀態重複提醒已稍後、已接單或已拒絕的訂單。Android `OnlineOrderNotifierPlugin` 背景輪詢 `/orders` 後會再查同一個 state endpoint，native snooze/markSeen 也會 POST 回 API，所以 foreground banner、Web fallback 與 native notification 使用同一個 suppression source。這個狀態是跨平板、跨 fresh reinstall 的來源，localStorage 只保留 station id 與既有 fallback 快取，不再承擔提醒去重。
- 線上點餐同步：消費者線上菜單讀 `/settings/runtime` 與 `/products?channel=online`，分類順序跟隨 `online_ordering.menuCategories`，商品順序跟隨 `/products` 依 `sort_order` 回傳的順序，頁首店家資訊與封面輪播讀 `online_ordering.storeProfile`，並依 runtime 註記設定顯示必填/選填選項與加價；`online_ordering.enabled=false` 時保留瀏覽但阻擋線上/QR 建單。POS 工具箱「線上點餐功能設定」可直接切換 `online_ordering.enabled`、`serviceModeAvailability` 與 `acceptanceRequired`，對齊 iCHEF POS 的全店同步營運方式；同一面板也會寫入目前平板的 `online_ordering.notificationRouting`，讓每台 iPad 的接收通知、提示音、服務方式、音量與內用桌位通知範圍分開保存。`online_ordering.serviceModeAvailability` 會分別控制自取、內用掃碼與外送，消費者頁會停用關閉的模式，`POST /orders` 也會重新載入 runtime 後拒絕 disabled service mode，避免只靠前端阻擋。`online_ordering.allowScheduledOrders` 搭配 `scheduledOrderIntervalMinutes`、`scheduledOrderMaxDays` 與 `scheduledOrderTimeWindows` 控制希望時間；消費者頁會限制 `datetime-local` 的最早/最晚/間隔並檢查星期時段，`POST /orders` 也會依台北時間重新驗證。`online_ordering.paymentMethods` 會依序顯示消費者付款方式，支援取餐時付款、LINE Pay、街口、線上刷卡、91APP 支付線上刷卡與轉帳；若某付款方式停用，消費者頁不顯示，`POST /orders` 也會拒絕繞過前端送出的 disabled payment method；每個付款方式也保存 `opensCashDrawer`，POS 收款完成後用同一份 runtime 判斷是否自動開錢櫃。外送另讀 `deliveryFeeAmount`、`deliveryMinimumSubtotal`、`freeDeliveryThreshold` 與 `deliveryTravelMinutes`；消費者頁會把外送費加入合計、用備餐時間加車程設定希望時間下限，並隱藏取餐時付款與轉帳，`POST /orders` 會依 runtime 重新計算門檻與免運，將運費覆寫到 `orders.extra_fee_amount`。`discount_settings` 會提供線上啟用的自動優惠，消費者頁與 `pos-api` 都用折抵後金額檢查外送門檻與免運。`online_ordering.checkoutInstructions` 會在結帳區顯示門市說明，`showTaxIdField` 與 `showCarrierBarcodeField` 分別控制統一編號與載具條碼欄位；前端與 `pos-api` 都會驗證統編為 8 碼數字、載具最多 32 字元。消費者頁訂閱 runtime/products Realtime event，並保留每 15 秒 fallback polling。櫃台草稿單走 `POST /orders/drafts` 與 `PATCH /orders/:id/draft`，草稿品項保存在 `orders.draft_lines`，正式結帳/出單走 `POST /orders/:id/finalize` 由 `finalize_pos_order()` 在同一 transaction 寫正式品項並扣庫存；線上建單與沒有草稿的櫃台建單仍可走 `POST /orders` 與 `create_pos_order()`。`GET /orders` 會把超過 `POS_PAYMENT_EXPIRY_MINUTES`（預設 20 分鐘）的線上/QR 待付款新單自動改成 `status=failed`、`payment_status=expired`；若 `online_ordering.acceptanceRequired=true`，線上/QR 新單會先顯示待接單通知，不進桌況頁佇列，按「查看訂單內容」可先看完整品項、顧客、統編與載具，按「接單」後才走 `POST /orders/:id/claim` 並排入佇列，按「拒絕接單」會呼叫未付款作廢或狀態更新並停止後續提醒。收款確認走 `PATCH /orders/:id/payment`，未收款作廢走 `POST /orders/:id/void`，已收款退款走 `POST /orders/:id/refund`，訂單狀態走 `PATCH /orders/:id/status`，列印工作會依後台規則拆成多筆 `POST /print-jobs`，列印單刪除走 `DELETE /print-jobs/:id`。API 失敗建立的櫃台單才會保存到 `script-coffee-pos-pending-orders`，佇列會標示「本機待同步」，後續同步成功時先補寫遠端並去重。
- iCHEF 預約單停售例外：2026-05-12 觀察 iCHEF 後台首頁公告「停售限當日訂單，預約單可正常供應」後，消費者線上/QR 頁會在選擇跨日希望時間時讓 `future_order_available=true` 且僅因 `sold_out_until` 暫停的商品重新可訂，並標示「預約可訂」。`pos-api` 也會重新查 `products`，拒絕停售、通路隱藏、庫存為 0 或未勾預約單可售的線上/QR 品項，不只靠前端菜單過濾。
- iCHEF 供應數量／狀態：2026-05-12 對照官方「設定商品供應數量／狀態」後，供應狀態頁在同一列提供 `--` 不限量與 `0-999` 可供應數量；輸入 `0` 或下單扣到 `0` 會由 `20260512253000_apply_supply_quantity_rules.sql` 的 products trigger 強制轉成全部停售。前端狀態切回正常或線上停售時，若原本數量為 0，會先改回不限量避免狀態與數量互相衝突。權限系統新增 `manageSupplyQuantityStatus`，商品管理角色會自動繼承，若在 protected permissions 開啟則單品、批次與註記供應狀態操作都會要求員工識別碼並留下既有 `access.verify` 稽核紀錄。
- 商品庫存欄位：`products.inventory_count`、`low_stock_threshold`、`sold_out_until` 由 `20260429110000_add_product_inventory_controls.sql` 新增；前端仍保留 `is_available` 作為人工上架/停售開關，庫存為 0 或暫停到期前會在 POS 端視為不可點。
- 商品數量直接輸入：2026-05-12 對照 iCHEF 官方「商品大數量銷售的調整方式」後，點餐頁商品方塊與票券品項保留鍵盤輸入數量；`usePosSession()` 會以同一商品的所有 CartLine 彙總後套用 `products.inventory_count` 上限，讓標準品項、不同註記變體與套餐父商品的直接輸入或加號操作都不會超過可售庫存。Vue 畫面的 `max` 與加號停用狀態只做即時提示，最終仍以 session helper 的 stock-aware clamp 為準。
- 庫存管理欄位：`inventory_categories`、`inventory_items`、`inventory_records` 由 `20260511235500_add_inventory_management.sql` 新增；`apply_inventory_record()` 在同一個資料庫 transaction 中鎖定庫存品項、計算進貨/退貨/消耗/報廢/盤點後的 `stock_quantity`，再插入操作紀錄。`inventory_consumption_rules` 由 `20260511235900_add_inventory_auto_consumption.sql` 新增，用 `subject_type=product|option` 分別保存商品與註記對庫存品項的自動消耗量；`create_pos_order()` 與 `finalize_pos_order()` 會在正式寫入訂單明細時套用規則並插入 `inventory_records.consumption`，暫停出單的明細不套用。這組表是 iCHEF 式庫存管理來源，與商品 `products.inventory_count` 分開保存，避免把原料/包材盤點和可售商品庫存混在一起。
- 下單扣庫存：`20260429150000_add_create_pos_order_function.sql` 新增 `create_pos_order()`，`POST /orders` 會透過 DB function 原子建單與扣庫存；`20260504090000_add_counter_order_drafts.sql` 新增 `orders.draft_lines` 與 `finalize_pos_order()`，讓櫃台先建草稿、跨平板追溯購物車，正式結帳/出單時再原子寫品項與扣庫存；庫存不足時整筆 rollback，POS 會移除暫存單並把品項還回購物車。
- 外送/履約欄位：`20260429161000_add_order_fulfillment_fields.sql` 新增 `orders.delivery_address` 與 `requested_fulfillment_at`，線上與櫃台建單都會寫入，POS 佇列與收據 payload 會顯示希望時間與地址。
- 外帶預約單保存：2026-05-12 對照 iCHEF 官方「新增外帶預約訂單」後，票券左上角日曆面板同時編輯顧客資料與 `requested_fulfillment_at`，並提供依 `engagement_settings.workflowAlerts.defaultTakeoutPickupMinutes` 帶入的預設取餐時間。`saveCounterOrder()` 可用 `{ queuePrint: false }` 保存櫃台單而不標記出單意圖；點餐頁「儲存後返回」會用這個路徑把電話/粉專預約外帶單送回佇列，保留取餐提醒與排序，但不建立 print job。
- 多平板鎖定欄位：`orders.claimed_by`、`claimed_at`、`claim_expires_at` 由 `20260429123000_add_order_claim_lease.sql` 新增；`pos-api` 只允許未鎖定、本機持有或已逾時的進行中訂單取得 claim，已交付/失敗/作廢單不可再 claim，狀態更新、收款與 print job 建立前都會檢查 lease，已交付/異常單會釋放 lease。
- 收銀班別欄位：`register_sessions` 由 `20260429133000_add_register_sessions.sql` 新增；`pos-api` 提供 `/register/current`、`/register/open`、`/register/close`，前端需先進入後台編輯模式才會開班/關班。`/register/close` 另需 `staffCode`，會用 `access_control.staffAccounts` 驗證啟用員工並把操作員姓名、角色與員工識別碼寫入 `pos_audit_events.register.close` metadata，對齊 iCHEF 關帳需記錄操作者的流程。
- 班別歷史與帳務補印：`pos-api` 提供 `/register/sessions?limit=`，會先依目前 station 解析多結帳口帳本，再回傳最近班別並附上目前摘要欄位。POS 關帳頁「瀏覽紀錄 / 補印」不新增 localStorage 狀態，載入 `/register/current` 時會同步班別歷史，補印小結、關帳與銷售紀錄會使用 `buildRegisterSessionReportPayload()` 產生 EZPL；關帳單僅允許已關班紀錄，補印前走後台編輯模式與 `manageCashDrawer` 權限驗證。
- 現金臨時收支：`register_cash_adjustments` 由 `20260510113000_add_register_cash_adjustments.sql` 新增，RLS 禁止前端直連讀寫，固定透過 `POST /register/cash-adjustments` 寫目前 open register。`/register/current` 會把 `cashAdjustmentIncome`、`cashAdjustmentExpense` 與最近 60 筆 `cashAdjustments` 一起回傳，開班中的 `expectedCash` 會動態算入臨時收入/支出；新增異動後 DB trigger 只 touch `register_sessions.updated_at`，沿用既有 `register_sessions` realtime topic 讓其他平板重拉班別資料。
- 關帳異常欄位：`register_sessions.open_order_count`、`failed_payment_count`、`failed_print_count`、`voided_order_count` 由 `20260429140500_add_register_closeout_exception_counts.sql` 新增；開班中的 `/register/current` 會動態重算，關班時會保存快照。有未交付、付款異常或列印失敗時，`/register/close` 需送 `force=true` 才能關班。
- 操作稽核：`pos_audit_events` 由 `20260429142000_add_pos_audit_events.sql` 新增；`pos-api` 會記錄建單、claim、釋放、狀態更新、收款、付款逾期、退款、作廢、商品/設定異動、庫存類別/品項/操作、自動消耗規則、會員建立、會員券建立/退回、錢包調整、開班、關班、現金臨時收支與錢櫃開啟事件。商品異動會寫入庫存、低庫存門檻、售價、上下架與暫停供應的前後值/差額；庫存管理會寫入 `inventory.record.purchase/return/consumption/scrapped/count`、`inventory.consumption_rule.*` 與操作後存量，後台 `/admin/audit-events` 可用來追帳與排錯。
- 會員錢包：`20260429154000_add_member_wallet_functions.sql` 新增 `create_pos_member()` 與 `adjust_pos_member_wallet()`；後台 `GET/POST /admin/members` 可建立/查詢會員，`POST /admin/members/:id/wallet-adjustments` 會在單一 DB transaction 內更新 `members.wallet_balance` 並寫入 `transaction_ledger`。
- CRM / 訂位 / 優惠券 / 發票欄位：`20260509010000_add_ichef_parity_controls.sql` 會替 `members` 補電話、顧客類型與點數，替 `orders` 補訂單標籤、服務費、其他費用、優惠折抵、點數折抵、券碼與獲得點數，替 `products` 補供應時段與跨日預約可售；新增 `member_coupons`、`reservations` 與 `engagement_settings` runtime setting。`20260512052000_add_product_total_display_settings.sql` 會在 `engagement_settings.productTotalDisplay` 補上 iCHEF 商品總數顯示開關與不計算分類/品項；`20260512093000_add_service_charge_runtime_settings.sql` 會在 `engagement_settings.serviceCharge` 補上 iCHEF 服務費設定，支援通路費率、折扣計算基準與不計算分類/品項；`20260512180000_add_member_points_ledger.sql` 會在 `engagement_settings.loyaltyPoints` 補點數活動預設，並新增 `member_points_ledger` 作為會員點數扣抵、累點與退款回補來源。POS 點餐票券、付款摘要與訂單明細用 product total 設定計算「商品總數」，但 GoDEX 已裁貼紙序號仍使用 `printer_settings.rules[].countExcludedCategories/countExcludedItemIds`，兩者不可共用以符合 iCHEF 官方知識庫說明。`20260512024500_add_coupon_redemption_state.sql` 再補 `member_coupons.redeemed_order_id`、`redeemed_at` 與 `redemption_station_id`，讓會員優惠券的已使用/退回狀態由資料庫共享，不靠 localStorage。`20260510143000_add_reservation_blacklist_entries.sql` 新增 `reservation_blacklist_entries`，用 `normalized_phone` 作唯一比對鍵並由 RLS 禁止前端直連；`20260510170000_add_reservation_table_assignment.sql` 新增 `reservations.assigned_table_ids`，讓公開線上訂位能持久保存分配桌號並用 GIN index 查詢。`20260511142000_add_order_invoice_checkout_fields.sql` 新增 `orders.tax_id` 與 `orders.invoice_carrier_barcode`，用 DB check constraint 擋下非 8 碼統編與過長載具。`reservationWebsite.specialDates` 直接存在既有 `engagement_settings` JSON runtime 中，不需要新增資料表；`pos-api` normalizer 會替舊設定補空陣列。`pos-api` 新增 `/members/search`、`/admin/coupons`、`/admin/reservations`、公開 `/reservations`、`/admin/reservation-blacklist`，並讓 `/settings/runtime` 回傳 `engagementSettings`、`reservationWebsite` 訂位網站規則與 `online_ordering` 結帳發票欄位設定。POS 工具箱的「訂位管理」直接讀寫這些 reservation 端點，不新增本機資料來源。
- 拆單欄位：`20260511202000_add_order_payment_splits.sql` 新增 `orders.payment_splits jsonb not null default '[]'` 與 array check constraint。此欄保存子單 `id/label/amount/lineKeys/paymentMethod/status/paidAt`，讓未正式結帳的草稿單與正式訂單都能跨平板還原拆單結果。
- 混合支付欄位：`20260511213000_add_order_mixed_payments.sql` 新增 `orders.payment_breakdown jsonb not null default '[]'` 與 `transaction_receipt_count integer default 0`。`payment_breakdown` 保存 `id/paymentMethod/amount/status/paidAt`，`transaction_receipt_count` 限制 0-10 張，供結帳後自動列印交易明細與 fresh reinstall 還原。
- 優惠活動設定：`20260512013000_add_discount_settings.sql` 新增 `pos_settings.key='discount_settings'` 預設值。設定本身放在 Supabase runtime，不新增本機資料來源；訂單仍保存最後 `discount_amount` 與稽核 metadata，活動規則由 runtime 與 `pos-api` 在建單/草稿/finalize 時重新計算。
- 套餐商品欄位：`20260512070000_add_combo_product_runtime_settings.sql` 會替 `online_ordering` runtime 補 `comboProductAssignments`，並在 `order_items` 新增 `combo_items jsonb not null default '[]'`。`orders.draft_lines` 保存草稿套餐子商品，正式明細則保存於 `order_items.combo_items`；`create_pos_order()` 與 `finalize_pos_order()` 會在同一個 transaction 中處理套餐子商品庫存與自動耗料。`20260512083000_add_combo_child_option_consumption.sql` 另補 `apply_combo_option_consumption_for_order_item()` trigger，讓套餐子商品註記也走 `inventory_consumption_rules.subject_type='option'` 的耗料規則。
- 金流 webhook：`20260429172000_add_payment_webhook_events.sql` 新增 `payment_events` 與 `record_pos_payment_event()`；`POST /payments/webhook/:provider` 需 `POS_PAYMENT_WEBHOOK_SECRET`，以 provider + event id 做冪等，金額不符只記錄不改單，已付款訂單不會被失敗/逾期回呼降級，退款回呼會寫負數交易流水。後台 `GET /admin/payment-events` 可讀最近回呼、重送與未套用事件，並支援 provider/狀態篩選與 CSV 匯出，方便正式串 LINE Pay / 街口前排錯。
- RPC 權限：`20260429183000_lock_down_pos_security_definer_rpc.sql` 撤掉 `create_pos_order()`、`refund_pos_order()`、`create_pos_member()`、`adjust_pos_member_wallet()` 與 `record_pos_payment_event()` 對 `public/anon/authenticated` 的 `EXECUTE`，只保留 `service_role`，讓外部請求固定經過 `pos-api` 的 webhook secret 與輸入驗證。
- 金流事件 RLS：`20260429183500_lock_down_payment_events_rls.sql` 補上 `payment_events` 的 no-direct-client policy，避免 anon/authenticated 直接讀寫 webhook 原始事件；後台查詢仍固定走 `pos-api` service role。
- 營運日報：`GET /admin/reports/daily?date=YYYY-MM-DD` 依台灣日界線彙總當日訂單，回傳實收、待收、退款、異常、付款方式、訂單來源、服務方式、時段分布與熱門商品；後台報表頁直接讀此端點。
- 退款沖銷：`20260429143000_add_refunded_payment_status.sql` 新增 `payment_status=refunded`；`20260429143500_add_refund_pos_order_function.sql` 新增 `refund_pos_order()`，讓退款在同一個資料庫 transaction 內更新訂單並寫入 `transaction_ledger`。
- 平板心跳：`pos_station_heartbeats` 由 `20260429144500_add_pos_station_heartbeats.sql` 新增；POS 工作台每 30 秒 upsert 一次，後台 `/admin/stations` 可看最後在線時間。
- 平板測試：`rtk npm run tablet:url` 會輸出同 Wi-Fi 平板可開啟的本機網址；瀏覽器版不能直連 TCP 出單機。
- APK 測試：已加入 Capacitor Android 專案、`Android APK` workflow、Android `LanPrinter` TCP socket plugin、`OnlineOrderNotifier` 背景新單提醒 plugin 與 `OnlineOrderPollingService` foreground service；本機 `rtk npm run apk:debug` 會先同步 Capacitor，macOS arm64 預設改用暫存 x86_64 Temurin 21 建置，並把 JVM fatal error 檔導到 `~/.cache/script-coffee-pos/`，避免 `hs_err_pid*.log` 污染專案資料夾。Android 13 以上首次進入 POS 會要求通知權限，若拒絕仍會保留前景提醒，但背景 notification 無法顯示；背景提醒測試需觀察 `OnlineOrderNotifier` log 內的 foreground service poll / notification signature。員工打卡實機測試需用 fresh reinstall 後重新開啟工具箱，確認打卡紀錄仍從資料庫載入而非本機快取。
- 列印計畫：`src/lib/printing.ts` 會依 `printer_settings` 的服務方式、品項分類、指定品項、貼紙/收據模式與 copies 建立多筆 EZPL payload；未被印單規則的分類或品項納入時不會列印，且 `CartLine.printPaused=true` 的明細會從收據與貼紙 payload 同步排除。前台列印站已整理為 iCHEF 式出單機清單、印單規則與列印佇列，印單規則可在列印站直接勾選分類與品項，底部「列印流程」提示已移除，不再承擔庫存/供應管理。分類 row 的名稱點擊只切換右側品項列表，checkbox 才改變整類選取；整類選取時右側該分類所有品項都顯示勾選，若取消單一品項，前端會移除 `rule.categories` 的該分類並把同分類其他品項寫進 `rule.itemIds`，避免 category OR itemIds 的列印判斷和畫面 checkbox 狀態錯位。2026-05-11 對照 iCHEF 後台與官方「已裁貼紙規則設定」補上 `countExcludedCategories` / `countExcludedItemIds`，列印範圍與「不計算商品」分開管理；貼紙序號會以該規則實際列印的總商品數計算並排除不計算品項，被排除的商品若仍在列印範圍內會照常列印但標示 `NC/總數`。逐筆暫停出單會保存在 `orders.draft_lines[].printPaused` 與 `order_items.print_paused`，重開 App、換平板或重印仍維持不補印，但結帳與庫存扣減照常。內用與外送舊收據規則載入時會正規化為「內用貼紙」「外送貼紙」。2026-05-10 依 iCHEF App 左下角 `⋯` 補上手動列印「訂單 QR Code」與「顧客聯」：兩者都沿用 claim、`print_jobs`、LAN printer 狀態回寫；QR code 使用 GoDEX EZPL `W...` QR command，URL 帶 `source=qr`、訂單與桌號參數，消費者頁收到 `source=qr` 會以 QR 訂單來源建立訂單。列印站 healthcheck 會分離畫面預覽與實際 EZPL payload，`usePosSession()` 逐筆建立/回寫 print job，列印頁會列出 print jobs 並可滑動刪除，Android APK 逐筆送 TCP。
- 本機驗證：2026-04-28 已跑 `rtk npm run ci-local` 通過；`rtk npm run pages:check` 在 Codex 沙盒內因 `dig` socket 與 GitHub API 網路限制無法二次驗證 HTTPS，需以一般終端、GitHub Pages UI 或 GitHub Actions 為準。

## 來源藍圖

參考文件：`/Users/kimi/Downloads/POS 系統藍圖更新：區域網路列印版.pdf`

藍圖重點：

- Samsung Tab A11+ 作為前台 POS 平板。
- GODEX DT2X 透過 RJ45 接店內 AP，固定內網 IP，例如 `192.168.1.100`。
- Web POS 使用 Vue 3 + Vite SFC，原始網頁部署到 GitHub Pages。
- Android 平板版本使用 Capacitor 封裝成 APK，debug APK 測試流程見 `docs/android-apk.md`。
- 區網列印用 Android `LanPrinter` Capacitor native plugin 繞過瀏覽器對本地 IP 的限制，直接送 EZPL 到出單機。
- 後端採 Supabase PostgreSQL + Deno/Hono Edge Functions。
- 外部整合包含 LINE Login、LINE Pay、街口支付。

## 沿用咖啡訂購專案的經驗

- 文件要分層：`README.md` 放人類可操作的入口，`DEV_CONTEXT.md` 放交接脈絡，長期規劃放 `docs/`。
- CI 要阻擋容易回到 legacy 的前端模式，所以這裡加入 `scripts/check_frontend_guardrails.py`。
- 表單與付款狀態要在使用者互動邊界就清楚建模，不只靠後端拒絕。
- 未來接 Supabase 時，資料流要從 API 回應、共享 types、view model 到 Vue template 一次打通。
- 金鑰與部署憑證只放 `.env.local`、GitHub Secrets 或 Supabase Secrets。

## 下一步

1. 在 Samsung Tab A11+ 安裝最新版 debug APK，對 GODEX DT2X 做 healthcheck label、櫃台訂單列印與關班流程實測。
2. 多平板 Realtime 後續：第一版已接 orders/runtime/register/products invalidation 與 polling fallback；下一步用兩台平板實測 claim lease、線上接單提醒、供應設定同步與斷線重連。
3. 接 LINE Login / LINE Pay / 街口支付時，把 provider 簽章驗證與 payload mapping 接到既有 `/payments/webhook/:provider` 契約，並用後台「支付事件」追查重送、金額不符與未套用回呼。
