import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.scriptcoffeeshop.pos',
  appName: 'Script Coffee POS',
  webDir: 'dist',
}

export default config
